apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: addons
  namespace: argocd
spec:
  generators:
    - matrix:
        generators:
          - list:
              elements:
                - app:
                    component: prometheus
                    branch: anguyen/argo-helm
                    namespace: ngsa
          - clusters: {}
  template:
    metadata:
      name: '{{app.component}}-{{name}}'
      labels:
        component: '{{app.component}}'
    spec:
      project: default
      sources:
        - repoURL: https://github.com/retaildevcrews/ngsa-asb
          targetRevision: '{{app.branch}}'
          path: spikes/argo-helm/deploy/{{app.component}}
          helm:
            valueFiles:
            - $values/spikes/argo-helm/clusters/{{name}}/values.yaml
        - ref: values
          repoURL: https://github.com/retaildevcrews/ngsa-asb
          targetRevision: '{{app.branch}}'
      destination:
        server: '{{server}}'
        namespace: '{{app.namespace}}'
      syncPolicy:
        syncOptions:
        - CreateNamespace=true
        automated:
          selfHeal: true
          prune: true